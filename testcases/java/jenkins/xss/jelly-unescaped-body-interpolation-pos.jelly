<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <!-- unescaped getter interpolation in element body -> stored XSS -->
  <div class="tpl">
    <span class="name">${instance.label}</span>
    <span class="chip">${it.tag}</span>
    <h3>${instance.getDisplayName()}</h3>
  </div>
</j:jelly>